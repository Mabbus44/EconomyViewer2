TODO:
MVP
* Change "account name" to "Transaction group" on transaction groups page
* Remove account number as match condition field
* "Discard changes" button on match conditions page
* "Discard changes" button on transaction groups page
* Add apply match condition button to transaction view
* Save to file
* Load from file
* View graph page
* Select transaction groups on view graph page
* Select start/end date on view graph page
* Show graph on view graph page

Others
* And "and" functionality for a transactiongroup that selects a list of matchConditions and "and"s them instead of the default "or"
* Add button on matchconditions page to "and" selected matchConditions
* Add uneditable column "and group" to match conditions page. so you can see which match conditions belong together
* Checkbox to only show pending changes
* Make transactions table fit columns, but max window width
* Check that dates is an actual date and not just any numbers in format xxxx-xx-xx
* Group multiple transaction groups on transaction groups page
* Add "last x years", "last x months", "last x calendar years" as quick x axis selections in graph page
* Add "month average", "3 month average", "year average" as selections in graph page
* Namme and save graph settings in graph page (part of the data in the normal save, not a separate file).

v0.05
* Add transactionsGroups and matchConditions pages
* Create core::TransactionGroup class and add it to transactionGroups view.
* Create core::MatchConditions class, and add it to matchConditions view.
* Update comparator column when field column is changed in matchConditions view
* Save matchCondition

v0.04
* Color edited transactions yellow
* Make core transaction vector into dict
* Color newly added (unapplied) transactions green
* Update color when transactions are applied

v0.03
* Redesign, make views have a ref to core, instead of using qt method "connect"
* Redesign, newTransactions added to transaction view instead of core.
* Add uniqe ID for each transaction
* Apply changes button

v0.02
* Add accounts page
* Add accounts to transactions at import
* Add accounts column to main page
* Refactor, each view has their own openView method that handles data insead of doing it in core
* Create open view methods for transactions
* Create open view methods for newTransaction
* Create open view methods for accounts
* Create open view methods for transactionGroups


v0.01
* Load transactions from file
* Display transactions in "new transactions" view
* Check format for transaction table cells
* Save transactions from "new transactions" view to main transaction list
* Display main transaction list
